# 🎨 機能拡張 作業計画書 — データフォーマット互換のまま「面白さ」を追加する

**作成日**: 2026-08-01
**対象バージョン**: v2.0.3 → v2.1.0
**ステータス**: 完了（v2.1.0 実装済み）

---

## 1. 目的

日本縦断チャレンジアプリに、**LocalStorage のデータフォーマットを一切変更せず**、既存機能への影響を最小限に抑えた「見栄え・面白さ」の追加機能を導入する。バーチャルフィットネスチャレンジ系アプリ（The Conqueror、My Virtual Mission 等）や国内のウォーキングスタンプラリー事例を調査し、本アプリに移植可能なアイデアを選定した。

## 2. 前提・制約

### 2.1 「データフォーマットを変えない」の定義

- `japanJourneyTracker` キー配下の既存フィールド（`totalDistance` / `records` / `earnedBadges` / `reachedCities` / `streakDays` / `stats` 等）の**構造・型・意味を変更しない**。
- 新機能は原則、**既存データからの計算（派生値）だけで実現**する。永続化を伴わない表示専用機能を最優先とする。
- どうしても設定値の保存が必要な場合は、**既存の `data.settings` オブジェクト**（`Storage.saveSettings()` / `getSettings()` が既に存在）にのみ追記する。`Storage.load()` はデフォルト値とマージする実装のため、`settings` 内のキー追加は旧バージョンとの互換性を壊さない。
- エクスポート/インポート・クラウドバックアップの JSON 形式は現状のまま。古いバックアップファイルがそのまま読める状態を維持する。

### 2.2 既存機能への影響を出さないための方針

- 新機能は **新規 JS ファイル**（`js/fun-*.js` 等）として追加し、既存モジュールへの変更は「初期化呼び出しの1行追加」と「タブ/カード用の HTML 追加」に限定する。
- 既存の8タブ構成は維持。新機能は既存タブ内のカード追加、またはモーダルとして実装する。
- 外部ライブラリの新規追加はしない（Chart.js と Canvas API / SVG / CSS のみで実現）。
- Service Worker のキャッシュリストに新規ファイルを追加し、`CACHE_VERSION` を更新する。

## 3. 調査結果サマリ

| 出典 | 参考にした仕掛け |
|------|----------------|
| The Conqueror | チェックポイント到達で**バーチャル絵はがき**が届く／ルート上の現在地を**ストリートビュー**で見られる／マイルストーン演出 |
| My Virtual Mission / MedalMad | 地図上を進む旅の可視化、収集型メダル |
| 国内スタンプラリー事例（i完歩帖、プラリーご朱印帳 等） | **御朱印帳・スタンプ帳**形式のコレクション表示、ご当地要素 |
| 国内ランニングアプリ（アプリブ調査） | 音声・演出系のエンタメ要素、距離の面白い換算表示 |

## 4. 機能候補一覧

凡例 — 工数: S(半日) / M(1〜2日) / L(3日以上)、永続化: なし＝表示専用（データ完全不変）

### 🥇 フェーズ1: 表示専用・完全無害（永続化なし）

| # | 機能 | 概要 | 工数 |
|---|------|------|------|
| 1-1 | **距離のおもしろ換算** | 累計距離を「フルマラソン◯回分」「東京〜大阪◯往復」「地球一周の◯%」「富士山◯合目相当の高さ換算」等で表示。統計タブにカード追加。 | S |
| 1-2 | **ゴール到着予測日** | 直近28日の平均ペースから「次の都市」「ゴール札幌」への到着予測日を計算して表示。「このペースなら12月24日に札幌到着！」 | S |
| 1-3 | **絵はがきコレクション** | The Conqueror 方式。`reachedCities` から到達済み都市ごとに絵はがき風カード（SVG/CSS製、名所イラスト＋豆知識＋到達日）を自動生成しギャラリー表示。到達日は `records` の累計から逆算。 | M |
| 1-4 | **御朱印帳風スタンプ帳** | 到達都市を和風の朱印スタンプデザインで一覧表示。未到達はグレーの枠のみ。バッジタブ内に追加。 | M |
| 1-5 | **旅のリプレイ** | `records` を時系列再生し、地図上をマーカーが那覇から現在地まで走るアニメーション＋月ごとの距離をテロップ表示。「▶ 旅を振り返る」ボタン。 | M |
| 1-6 | **1年前の今日 / 思い出表示** | 記録フォーム付近に「1年前の今日は◯km 走りました（メモ: …）」を表示。`records` の日付検索のみ。 | S |
| 1-7 | **現在地をGoogleマップで見る** | 都市間の進捗率から緯度経度を線形補間し、Googleマップ/ストリートビューへの外部リンクを生成。「いま実際の日本のこのあたり」を体感できる。 | S |

### 🥈 フェーズ2: 演出・見栄え強化（永続化なし〜settings のみ）

| # | 機能 | 概要 | 工数 | 永続化 |
|---|------|------|------|--------|
| 2-1 | **季節テーマの地図演出** | 記録日の月に応じて地図に桜(春)・波(夏)・紅葉(秋)・雪(冬)の CSS パーティクルを重ねる。ON/OFF は `settings.seasonFx`。 | M | settings |
| 2-2 | **シェア画像ジェネレーター** | Canvas で「現在地・累計距離・レベル・進捗バー」入りの進捗カード画像 (PNG) を生成し、ダウンロード／Web Share API で共有。 | M | なし |
| 2-3 | **効果音・ファンファーレ** | 記録時・都市到達時に Web Audio API で短い効果音（外部音源ファイル不使用、コード生成）。ON/OFF は `settings.sound`。 | S | settings |
| 2-4 | **紙吹雪の強化＋都市別演出** | 都市到達モーダルの紙吹雪を都市テーマ色に（沖縄=ハイビスカス色、札幌=雪の結晶 等）。 | S | なし |

### 🥉 フェーズ3: 遊び・やり込み要素（永続化なし〜settings のみ）

| # | 機能 | 概要 | 工数 | 永続化 |
|---|------|------|------|--------|
| 3-1 | **ご当地クイズ** | 到達済み都市の名所・名物の3択クイズ。出題データは `routes.js` の trivia を拡張した静的定義（コード内定数のため保存データ不変）。正答状況は保存しない揮発式 or `settings.quizScore`。 | M | 任意 |
| 3-2 | **ゴーストペーサー** | 「1日5kmの旅人」等の仮想ライバルを設定し、地図上に半透明マーカーで表示。「ゴーストより 23km リード中！」。開始日と日次ペースは `settings.ghost` に保存。 | M | settings |
| 3-3 | **年間振り返り（Wrapped風）** | 年末に `records` から「今年の総距離・最長の日・最も走った月・獲得バッジ数」をスライドショー形式で自動生成。 | L | なし |
| 3-4 | **今日のミッション（おみくじ）** | 日付シードの擬似乱数で「今日は+1kmチャレンジ」「メモに空の写真を」等の軽いお題を毎日1つ表示。達成判定はその日の records から計算。 | S | なし |

## 5. 実装スケジュール（案）

| 週 | 作業内容 | 成果物 |
|----|---------|--------|
| 第1週 | フェーズ1着手: 1-1, 1-2, 1-6, 1-7（計算系・小粒4本） | `js/fun-stats.js`、統計タブへのカード追加、単体テスト |
| 第2週 | フェーズ1続き: 1-3 絵はがき、1-4 スタンプ帳 | `js/postcards.js`、`css/postcards.css` |
| 第3週 | 1-5 旅のリプレイ ＋ フェーズ2: 2-2 シェア画像、2-4 紙吹雪強化 | `js/replay.js`、`js/share-card.js` |
| 第4週 | フェーズ2残り（2-1 季節演出、2-3 効果音）、SW キャッシュ更新、README 更新、リリース (v2.1.0) | リリースタグ |
| 以降 | フェーズ3は反応を見て個別に判断 | — |

各機能は独立しているため、**1機能 = 1 PR** とし、途中でやめても他機能に影響しない構成とする。

## 6. 技術方針

- **新規ファイル構成（予定）**: `js/fun-stats.js`（換算・予測・思い出）、`js/postcards.js`（絵はがき・スタンプ帳）、`js/replay.js`、`js/share-card.js`、`js/season-fx.js`、`css/fun.css`
- 到達日の逆算ロジック: `records` を日付順にソートし累計距離を積み上げ、各都市の `cumulative` を超えた日を「到達日」とする純関数として実装（テスト容易）。
- 位置補間: `routes.js` の各都市に緯度経度定数を**コード内に**追加（`ROUTE_DATA` はコードであり保存データではないため、フォーマット制約に抵触しない）。
- 効果音は Web Audio API のオシレーターで生成し、音声ファイルを持たない（PWA キャッシュ肥大防止）。
- 画像生成は Canvas 2D のみ。写真データ（既存の `photo` フィールド）は読み取りのみで加工保存しない。

## 7. テスト・検証方針

- 到達日逆算・換算計算・到着予測は `tests/` に Jest 単体テストを追加（既存テスト構成に準拠）。
- **互換性検証（必須）**: v2.0.3 でエクスポートした JSON を新バージョンでインポート → 全機能動作 → 再エクスポートした JSON が v2.0.3 で読めることを確認。
- 新機能の JS を1本無効化してもアプリが起動すること（グレースフルデグラデーション）を各 PR で確認。
- Firefox / Safari（File System Access API 非対応環境）でも表示専用機能が動作することを確認。

## 8. リスクと対策

| リスク | 対策 |
|--------|------|
| `settings` 追記が想定外の上書きを起こす | `saveSettings` は既存実装がスプレッドマージのため追記安全。テストで確認 |
| Canvas 画像生成で `photo`（Base64）が大きくメモリを圧迫 | シェア画像には写真を含めない設計にする |
| 演出過多で動作が重くなる | パーティクルは CSS アニメーション＋要素数上限、設定で OFF 可能に |
| SW キャッシュ更新漏れで新ファイルがオフライン時に読めない | リリースチェックリストに `sw.js` の `CACHE_VERSION` 更新を明記 |

## 9. 参考資料

- [The Conqueror Virtual Challenges](https://www.theconqueror.events/) — 絵はがき・ストリートビュー・マイルストーン演出
- [The Conqueror — チャレンジの選び方（絵はがき機能の説明）](https://www.theconqueror.events/how-to-choose-the-best-virtual-challenge-for-you/)
- [My Virtual Mission](https://www.myvirtualmission.com/) — 地図上のバーチャルジャーニー
- [300+ Virtual Walking Apps (walkathonvirtual.com)](https://www.walkathonvirtual.com/virtual-walking-challenge-app/) — バッジ・実績・進捗トラッキングの整理
- [面白いランニングアプリおすすめ6選（アプリブ）](https://app-liv.jp/sports/rikujou/3311/) — エンタメ系機能の事例
- [ウォーキング・ハイキングコース スタンプラリー i完歩帖](https://ikanpo.wstv.jp/original.html) — スタンプ帳型コレクション
- [プラリーご朱印帳（日本スタンプラリー協会）](https://stamprally.org/s/43748) — 御朱印帳デジタルスタンプの事例

## 10. 実装結果（2026-08-01）

フェーズ1〜3の全15機能をv2.1.0として実装した。今回の作業指示に合わせ、当初の「1機能 = 1 PR」方針は変更し、全機能を1つの機能ブランチと1件のPRにまとめた。

### 成果物

- `js/fun-stats.js`: 距離換算、到着予測、1年前の今日、緯度経度補間、今日のミッション
- `js/postcards.js`: 到達日逆算、絵はがき、御朱印帳
- `js/replay.js`: recordsの時系列再生
- `js/share-card.js`: Canvas進捗カード、PNG保存、Web Share
- `js/season-fx.js`: 季節パーティクルと設定
- `js/sound-effects.js`: Web Audio効果音と設定
- `js/fun-extras.js`: ご当地クイズ、ゴーストペーサー、年間振り返り、都市別紙吹雪テーマ
- `css/fun.css`: 全機能のレスポンシブ／ダークモード対応スタイル

### 互換性・検証

- `japanJourneyTracker` の既存フィールド構造・型・意味は変更していない。
- 永続化する新規値は `settings.seasonFx` / `settings.sound` / `settings.ghost` のみ。
- v2.0.3バックアップのインポート→再エクスポート→旧形式読込テストを追加した。
- 新規JS 7本を1本ずつ無効化しても起動を継続するテストを追加した。
- Service Workerのキャッシュを `v2.1.0` に更新し、新規JS/CSS/SVGをプリキャッシュ対象に追加した。
- デスクトップとスマートフォン幅で主要画面・操作をブラウザ確認した。
